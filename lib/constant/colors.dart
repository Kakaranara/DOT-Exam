import 'package:flutter/cupertino.dart';

class ThemeColor {
  static Color blue({double opacity = 1}) {
    return Color.fromRGBO(10, 151, 171, opacity);
  }

  static Color green({double opacity = 1}) {
    return Color.fromRGBO(70, 181, 167, opacity);
  }
}
